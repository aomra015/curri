<div class="branding-colors">
  <h4>Main app color</h4>
  <p>This is the color that will be used prominently in the landing page as well as the background for the navigation menu. It forms the base for all body text colors that are not white.</p>
  <div class="main">Main</div>
  <h4>Secondary colors</h4>
  <p>These are the colors that form the bases for buttons and other elements where colors are needed, such as progress bars and alerts. Sass functions <code>lighten()</code> and <code>darken()</code> are used to create the variations required.</p>
  <div class="primary">Primary</div>
  <div class="success">Success</div>
  <div class="warning">Warning</div>
  <div class="danger">Danger</div>
  <h4>Grays</h4>
  <p>There are 2 grays defined and should be sufficient to be used as is or with variations created by using <code>lighten()</code> and <code>darken()</code> Sass functions.</p>
  <div class="lt-grey">Light Grey</div>
  <div class="md-grey">Medium Grey</div>
</div>