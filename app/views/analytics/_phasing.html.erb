<%= form_tag classroom_track_analytics_path(@classroom, @track), method: "get", id: 'phase-selector' do  %>
  <%= select_tag :phase_state, options_for_select(Phase::PHASES, @phase.state) %>
  <noscript>
    <%= submit_tag 'Choose phase', name: nil, class: 'btn btn-small btn-primary'%>
  </noscript>
<% end %>