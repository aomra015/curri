<div class="progress">
  <%= render_bar(ratings_count(ratings, 0), 0, percent_score(ratings, 0) )%>
  <%= render_bar(ratings_count(ratings, 1), 1, percent_score(ratings, 1) )%>
  <%= render_bar(ratings_count(ratings, 2), 2, percent_score(ratings, 2) )%>
</div>